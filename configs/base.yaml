defaults:
  - compartmental: sir
  - network: test
  - training: test
  - _self_

simulation:
  t_span: [0, 50]
  t_eval_points: 1000

plotting:
  title: "Compartmental Model Simulation"
  show_plot: true
  save_plot: true

data:
  save_data: true

seed: 42
device: "cpu"
