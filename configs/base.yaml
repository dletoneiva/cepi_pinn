defaults:
  - compartmental: sir
  - network: test
  - training: test
  - _self_

simulation:
  t_span: [0, 50]
  t_eval_points: 1000
  solve_ode: true

plotting:
  title: "Compartmental Model Simulation"
  show_plot: true
  save_plot: true

data:
  save_data: true
