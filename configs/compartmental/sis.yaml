# SIS (Susceptible-Infected-Susceptible) model configuration
model:
  type: "SISModel"
  parameters:
    beta: 0.3   # infection rate
    gamma: 0.1  # recovery rate (individuals become susceptible again)
  initial_conditions: [0.99, 0.01]  # S(0), I(0)
  output_size: 2
