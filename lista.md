# Próximos passos
- Logar com hydra os experimentos (colocar um markdown na pasta do resultado)
- Comandos de salvar figura pelo hydra (conferir todos). salvar no diretorio certo
- Consertar figuras com labels pequenos


# comandos
docker compose run --rm app python3 -m pinn_epi.experiments.run_simulation --config-name=si_base